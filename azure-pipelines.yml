# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'
- script: sudo apt-get install perl build-essential
  displayName: 'Install Perl'
- script: perl -E"say q{hello world}"
  displayName: 'Hello World via Perl'
- script: cpanm --installdeps List::Cycle
  displayName: 'Install List::Cycle'
- script: cpanm --installdeps .
  displayName: 'Install modules with cpanm'
